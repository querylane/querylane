---
title: Schema service
description: "Service for managing schemas within databases."
sidebar:
  label: Schema
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

Service for managing schemas within databases.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `SchemaService`
- **Stability:** `v1alpha1`
- **Source:** [`schema.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/schema.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.
The JSON examples use protobuf JSON field names and wire encodings. They are request starting points, not production credentials or guaranteed resources.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`ListSchemas`](#listschemas) | Unary | `ListSchemasRequest` | `ListSchemasResponse` |
| [`GetSchema`](#getschema) | Unary | `GetSchemaRequest` | `GetSchemaResponse` |

## ListSchemas

Lists schemas available in a given Database.

```protobuf
rpc ListSchemas(ListSchemasRequest) returns (ListSchemasResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.SchemaService/ListSchemas`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "parent": "instances/production/databases/app",
  "pageSize": 50
}
```

### Request

`ListSchemasRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | Required | Required. The parent Database from which to list schemas. Format: instances/\{instance\}/databases/\{database\} |
| `pageSize` | `int32` | Optional | Optional. The maximum number of schemas to return. Server may return fewer. If unspecified or 0, server will choose a reasonable default (typically 50). Maximum value is 1000. |
| `pageToken` | `string` | Optional | Optional. A page token received from a previous `ListSchemas` call. Provide this to retrieve the next page of results. |
| `filter` | `string` | Optional | Optional. Filter expression (an AIP-160 subset: `=`, `!=`, the `:` case-insensitive substring operator, combined with `AND`). Values are quoted strings or the bare bools true/false. Filterable fields: - name, owner (string; support the `:` substring operator) - is_system_schema (bool) Examples: - is_system_schema = false (exclude system schemas) - owner = "postgres" - name:"app" |
| `orderBy` | `string` | Optional | Optional. Ordering expression following AIP-132 syntax. Supported fields: name, display_name, owner, last_ddl_time Default: "name asc" Examples: - "name asc" - "last_ddl_time desc, name asc" - "owner desc" |

### Response

`ListSchemasResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `schemas` | `repeated Schema` | — | The list of schemas found in the database. |
| `nextPageToken` | `string` | — | A token to retrieve the next page of results. If this field is empty, there are no subsequent pages. |
| `syncMetadata` | `CatalogSyncMetadata` | Output only | Output-only. Freshness and sync status for this catalog list. |

## GetSchema

Gets the details of a specific Schema.

```protobuf
rpc GetSchema(GetSchemaRequest) returns (GetSchemaResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.SchemaService/GetSchema`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "name": "instances/production/databases/app/schemas/public"
}
```

### Request

`GetSchemaRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. The name of the schema to retrieve. Format: instances/\{instance\}/databases/\{database\}/schemas/\{schema\} |

### Response

`GetSchemaResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `schema` | `Schema` | — | The retrieved schema resource. |
