---
title: Extension service
description: "Service for viewing PostgreSQL extensions available in a database."
sidebar:
  label: Extension
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

Service for viewing PostgreSQL extensions available in a database.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `ExtensionService`
- **Stability:** `v1alpha1`
- **Source:** [`extension.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/extension.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.
The JSON examples use protobuf JSON field names and wire encodings. They are request starting points, not production credentials or guaranteed resources.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`ListExtensions`](#listextensions) | Unary | `ListExtensionsRequest` | `ListExtensionsResponse` |

## ListExtensions

Lists extensions available in a given Database, including whether each extension is installed in that database.

```protobuf
rpc ListExtensions(ListExtensionsRequest) returns (ListExtensionsResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.ExtensionService/ListExtensions`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "parent": "instances/production/databases/app",
  "pageSize": 50
}
```

### Request

`ListExtensionsRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | Required | Required. The parent Database from which to list extensions. Format: instances/\{instance\}/databases/\{database\} |
| `pageSize` | `int32` | Optional | Optional. The maximum number of extensions to return. Server may return fewer. If unspecified or 0, server will choose a reasonable default (typically 50). Maximum value is 1000. |
| `pageToken` | `string` | Optional | Optional. A page token received from a previous `ListExtensions` call. |
| `filter` | `string` | Optional | Optional. Filter expression (an AIP-160 subset: `=`, `!=`, the `:` case-insensitive substring operator, combined with `AND`). Values are quoted strings or the bare bools true/false. Filterable fields: - name, schema (string; support the `:` substring operator) - installed (bool) Examples: - installed = true - name:"trgm" - schema = "public" AND installed = true |
| `orderBy` | `string` | Optional | Optional. Ordering expression following AIP-132 syntax. Supported fields: name, schema, installed Default: "name asc" |

### Response

`ListExtensionsResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `extensions` | `repeated Extension` | — | The list of extensions available in the database. |
| `nextPageToken` | `string` | — | A token to retrieve the next page of results. If this field is empty, there are no subsequent pages. |
