---
title: Table data service
description: "TableDataService exposes live row reads against a single Table resource."
sidebar:
  label: Table data
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

TableDataService exposes live row reads against a single Table resource. Methods are AIP-136 custom methods on the Table resource — requests use `name`, not `parent`. Distinct from TableService (catalog) and SQLService (ad-hoc SQL) because the three surfaces have different cost, timeout, pooling, and authorization profiles.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `TableDataService`
- **Stability:** `v1alpha1`
- **Source:** [`table_data.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/table_data.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`ReadRows`](#readrows) | Unary | `ReadRowsRequest` | `ReadRowsResponse` |
| [`StreamRows`](#streamrows) | Server stream | `StreamRowsRequest` | `StreamRowsResponse` |
| [`ReadCellValue`](#readcellvalue) | Unary | `ReadCellValueRequest` | `ReadCellValueResponse` |

## ReadRows

Reads a single bounded page of rows. Intended for the interactive grid.

```protobuf
rpc ReadRows(ReadRowsRequest) returns (ReadRowsResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.TableDataService/ReadRows`
- **Mode:** Unary

### Request

`ReadRowsRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. The table to read from. Format: instances/\{instance\}/databases/\{database\}/schemas/\{schema\}/tables/\{table\} |
| `pageSize` | `int32` | Optional | Optional. Default 50, hard cap 500. |
| `pageToken` | `string` | Optional | Optional. Opaque cursor from a previous response (AIP-158). Bound to (table, projection, filter, order, page_size); mismatch or expiry returns INVALID_ARGUMENT. |
| `selectedColumns` | `repeated string` | — | Optional. Subset of columns. Empty = all columns in catalog ordinal order. |
| `orderBy` | `repeated RowOrder` | — | Optional. Compound order. Server appends identity columns internally as a tiebreaker; clients neither see nor need to provide it. |
| `filter` | `RowFilter` | Optional | Optional. Recursive filter tree. |
| `rowCountMode` | `RowCountMode` | Optional | Optional. Default NONE. |
| `cellValueMode` | `CellValueMode` | Optional | Optional. Default PREVIEW with server-default max_cell_bytes. |
| `maxCellBytes` | `int32` | Optional | Optional. Per-cell preview cap. 0 = server default (8 KiB), hard cap 1 MiB. For text/json/xml the value is interpreted as a character ceiling (PostgreSQL substring is character-based); resulting wire bytes may be up to 4× this for multi-byte UTF-8. Byte-exact for bytea. |
| `maxResponseBytes` | `int64` | Optional | Optional. Per-response wire-byte cap. 0 = server default (8 MiB), hard cap 32 MiB. When hit mid-page the server stops adding rows and returns next_page_token from the last included row. Clients must NOT treat "fewer rows than page_size" as "last page" — only an empty next_page_token signals the last page. |

### Response

`ReadRowsResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `resultSet` | `TableResultSet` | — | The retrieved result set. |
| `nextPageToken` | `string` | — | Empty means this is the last page. |
| `limits` | `ResponseLimits` | — | Effective server caps applied this call. |

## StreamRows

Streams rows for export and bulk processing. Emits metadata first, then row batches, then stats. Same filter/order grammar as ReadRows.

```protobuf
rpc StreamRows(StreamRowsRequest) returns (stream StreamRowsResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.TableDataService/StreamRows`
- **Mode:** Server stream

### Request

`StreamRowsRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. Same shape as ReadRowsRequest.name. |
| `selectedColumns` | `repeated string` | — | Optional. Subset of columns. |
| `orderBy` | `repeated RowOrder` | — | Optional. Compound order. |
| `filter` | `RowFilter` | Optional | Optional. Recursive filter tree. |
| `cellValueMode` | `CellValueMode` | Optional | Optional. Default PREVIEW. |
| `maxCellBytes` | `int32` | Optional | Optional. Per-cell preview cap. 0 = server default. |
| `maxRows` | `int64` | Optional | Optional. Hard ceiling on rows. 0 = server default (1,000,000). |
| `batchSize` | `int32` | Optional | Optional. Rows per batch. 0 = server default (1000). |
| `maxTotalBytes` | `int64` | Optional | Optional. Ceiling on cumulative response payload bytes streamed. 0 = uncapped (still bounded by max_rows + per-cell cap). Stream ends with stats.truncated=true if hit. Hard cap 16 GiB. |

### Response

`StreamRowsResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `metadata` | `StreamRowsMetadata` | One of event | Exactly one, first. |
| `batch` | `StreamRowsBatch` | One of event | Zero or more. |
| `stats` | `StreamRowsStats` | One of event | Exactly one, last. |

## ReadCellValue

Reads the full value of a single cell, identified by an opaque token returned in TableCell.full_value_token when a PREVIEW response truncated the value.

```protobuf
rpc ReadCellValue(ReadCellValueRequest) returns (ReadCellValueResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.TableDataService/ReadCellValue`
- **Mode:** Unary

### Request

`ReadCellValueRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. The table the cell belongs to. Format: instances/\{instance\}/databases/\{database\}/schemas/\{schema\}/tables/\{table\} |
| `fullValueToken` | `string` | Required | Required. Token from a TableCell.full_value_token. The server rejects a token whose payload table_name does not match `name` — this binds the token to a specific table and prevents cross-table replay. |
| `maxBytes` | `int64` | Optional | Optional. Max bytes to return. 0 = server default (16 MiB), hard cap 64 MiB. For text/json/xml the value is interpreted as a character ceiling (PostgreSQL substring is character-based); resulting wire bytes may be up to 4× this for multi-byte UTF-8. Byte-exact for bytea. |

### Response

`ReadCellValueResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `value` | `TableCell` | Output only | Output-only. truncated=true means even the full read still hit max_bytes; full_size_bytes reports the actual on-disk size. |
