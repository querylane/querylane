---
title: Admin service
description: "AdminService exposes operational introspection of querylane's own backend: live replicas, the background runner job queue including lease ownership, raw catalog sync state, and metrics sample storage."
sidebar:
  label: Admin
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

AdminService exposes operational introspection of querylane's own backend: live replicas, the background runner job queue including lease ownership, raw catalog sync state, and metrics sample storage. It is a debugging surface for operators ("why don't my metrics show up?") and is not yet gated by authorization; once authn/authz lands, access will be restricted to admins at the service level.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `AdminService`
- **Stability:** `v1alpha1`
- **Source:** [`admin.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/admin.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.
The JSON examples use protobuf JSON field names and wire encodings. They are request starting points, not production credentials or guaranteed resources.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`ListReplicas`](#listreplicas) | Unary | `ListReplicasRequest` | `ListReplicasResponse` |
| [`ListAdminRunnerExecutions`](#listadminrunnerexecutions) | Unary | `ListAdminRunnerExecutionsRequest` | `ListAdminRunnerExecutionsResponse` |
| [`ListCatalogSyncStates`](#listcatalogsyncstates) | Unary | `ListCatalogSyncStatesRequest` | `ListCatalogSyncStatesResponse` |
| [`GetMetricsStorageStats`](#getmetricsstoragestats) | Unary | `GetMetricsStorageStatsRequest` | `GetMetricsStorageStatsResponse` |

## ListReplicas

Lists querylane backend replicas known from their heartbeats.

```protobuf
rpc ListReplicas(ListReplicasRequest) returns (ListReplicasResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.AdminService/ListReplicas`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "pageSize": 50
}
```

### Request

`ListReplicasRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `pageSize` | `int32` | Optional | — |
| `pageToken` | `string` | Optional | — |
| `filter` | `string` | Optional | Optional. AIP-160 filter. Filterable fields: replica_id, hostname. |
| `orderBy` | `string` | Optional | Optional. AIP-132 order_by. Defaults to `replica_id`. |

### Response

`ListReplicasResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `replicas` | `repeated Replica` | — | — |
| `nextPageToken` | `string` | — | — |

## ListAdminRunnerExecutions

Lists background runner executions with lease ownership details. The admin counterpart of RunnerService.ListRunnerExecutions, which hides lease owners.

```protobuf
rpc ListAdminRunnerExecutions(ListAdminRunnerExecutionsRequest) returns (ListAdminRunnerExecutionsResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.AdminService/ListAdminRunnerExecutions`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "pageSize": 50
}
```

### Request

`ListAdminRunnerExecutionsRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `pageSize` | `int32` | Optional | — |
| `pageToken` | `string` | Optional | — |
| `filter` | `string` | Optional | Optional. AIP-160 filter. Filterable fields: runner_name, target. Example: `target = "instances/prod"` or `runner_name = "probe_cache"`. |
| `orderBy` | `string` | Optional | Optional. AIP-132 order_by. Defaults to `runner_name, target`. |

### Response

`ListAdminRunnerExecutionsResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `runnerExecutions` | `repeated AdminRunnerExecution` | — | — |
| `nextPageToken` | `string` | — | — |

## ListCatalogSyncStates

Lists catalog sync state per scope with raw, unsanitized errors.

```protobuf
rpc ListCatalogSyncStates(ListCatalogSyncStatesRequest) returns (ListCatalogSyncStatesResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.AdminService/ListCatalogSyncStates`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "pageSize": 50
}
```

### Request

`ListCatalogSyncStatesRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `pageSize` | `int32` | Optional | — |
| `pageToken` | `string` | Optional | — |
| `filter` | `string` | Optional | Optional. AIP-160 filter. Filterable fields: scope, status. |
| `orderBy` | `string` | Optional | Optional. AIP-132 order_by. Defaults to `scope`. |

### Response

`ListCatalogSyncStatesResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `catalogSyncStates` | `repeated CatalogSyncState` | — | — |
| `nextPageToken` | `string` | — | — |

## GetMetricsStorageStats

Reports storage consumed by the metrics sample tables and the configured retention period.

```protobuf
rpc GetMetricsStorageStats(GetMetricsStorageStatsRequest) returns (GetMetricsStorageStatsResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.AdminService/GetMetricsStorageStats`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{}
```

### Request

`GetMetricsStorageStatsRequest` has no fields.

### Response

`GetMetricsStorageStatsResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `sampleTables` | `repeated SampleTableStats` | Output only | Output-only. Per-table storage stats, one entry per sample table. |
| `retentionPeriod` | `Duration` | Output only | Output-only. Maximum age of retained samples; older rows are pruned by the sample_retention runner. |
