---
title: Onboarding service
description: "OnboardingService guides first-time users through configuring the application database."
sidebar:
  label: Onboarding
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

OnboardingService guides first-time users through configuring the application database. It exposes the available setup methods, executes setup, and streams real-time progress events back to the client.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `OnboardingService`
- **Stability:** `v1alpha1`
- **Source:** [`onboarding.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/onboarding.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.
The JSON examples use protobuf JSON field names and wire encodings. They are request starting points, not production credentials or guaranteed resources.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`GetOnboardingState`](#getonboardingstate) | Unary | `GetOnboardingStateRequest` | `GetOnboardingStateResponse` |
| [`SetupAppDatabase`](#setupappdatabase) | Server stream | `SetupAppDatabaseRequest` | `SetupAppDatabaseResponse` |
| [`WatchConfigChanges`](#watchconfigchanges) | Server stream | `WatchConfigChangesRequest` | `WatchConfigChangesResponse` |

## GetOnboardingState

Returns the current onboarding state including available setup methods, filesystem writability, and database status.

```protobuf
rpc GetOnboardingState(GetOnboardingStateRequest) returns (GetOnboardingStateResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.OnboardingService/GetOnboardingState`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{}
```

### Request

`GetOnboardingStateRequest` has no fields.

### Response

`GetOnboardingStateResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `isConfigured` | `bool` | Output only | Output-only. Whether a database configuration already exists. |
| `appDatabaseStatus` | `AppDatabaseStatus` | Output only | Output-only. Current status of the application database. |
| `homePath` | `string` | Output only | Output-only. Absolute path to the querylane home directory (~/.querylane). |
| `isHomeWritable` | `bool` | Output only | Output-only. Whether the home directory is writable. |
| `availableMethods` | `repeated SetupMethod` | Output only | Output-only. Setup methods available in the current environment. |
| `configFilePath` | `string` | Output only | Output-only. Absolute path to the configuration file. |
| `embeddedDataPath` | `string` | Output only | Output-only. Absolute path to the embedded PostgreSQL data directory. |

## SetupAppDatabase

Configures and initializes the application database using the provided connection details or embedded PostgreSQL settings. Returns a stream of progress events so the client can render a step-by-step UI.

```protobuf
rpc SetupAppDatabase(SetupAppDatabaseRequest) returns (stream SetupAppDatabaseResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.OnboardingService/SetupAppDatabase`
- **Mode:** Server stream

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "postgresConfig": {
    "host": "metadata-db.example.com",
    "port": 5432,
    "database": "querylane",
    "username": "querylane",
    "password": "replace-me",
    "sslMode": "SSL_MODE_VERIFY_FULL"
  }
}
```

### Request

`SetupAppDatabaseRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `postgresConfig` | `PostgresConfig` | One of setup | Configure using an external PostgreSQL connection. |
| `embeddedConfig` | `EmbeddedSetupConfig` | One of setup | Configure using the embedded PostgreSQL instance. |

### Response

`SetupAppDatabaseResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `event` | `SetupProgressEvent` | Output only | Output-only. The progress event for the current step. |

## WatchConfigChanges

Subscribes to database setup progress triggered by external config changes (for example, manual YAML edits detected by the file watcher). Returns a stream of progress events identical in shape to SetupAppDatabase.

```protobuf
rpc WatchConfigChanges(WatchConfigChangesRequest) returns (stream WatchConfigChangesResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.OnboardingService/WatchConfigChanges`
- **Mode:** Server stream

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{}
```

### Request

`WatchConfigChangesRequest` has no fields.

### Response

`WatchConfigChangesResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `event` | `SetupProgressEvent` | Output only | Output-only. The progress event for the current step. |
