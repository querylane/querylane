---
title: View service
description: "ViewService exposes views as a separate resource hierarchy."
sidebar:
  label: View
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

ViewService exposes views as a separate resource hierarchy.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `ViewService`
- **Stability:** `v1alpha1`
- **Source:** [`view.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/view.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.
The JSON examples use protobuf JSON field names and wire encodings. They are request starting points, not production credentials or guaranteed resources.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`ListViews`](#listviews) | Unary | `ListViewsRequest` | `ListViewsResponse` |
| [`GetView`](#getview) | Unary | `GetViewRequest` | `GetViewResponse` |

## ListViews

Lists views and materialized views in a schema.

```protobuf
rpc ListViews(ListViewsRequest) returns (ListViewsResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.ViewService/ListViews`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "parent": "instances/production/databases/app/schemas/public",
  "pageSize": 50
}
```

### Request

`ListViewsRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | Required | Required. The parent schema from which to list views. Format: instances/\{instance\}/databases/\{database\}/schemas/\{schema\} |
| `pageSize` | `int32` | Optional | — |
| `pageToken` | `string` | Optional | — |
| `filter` | `string` | Optional | Optional. Filter expression (an AIP-160 subset: `=`, `!=`, the `:` case-insensitive substring operator, combined with `AND`). Values are quoted strings. Filterable fields: - name (string; supports the `:` substring operator) Example: name:"daily" |
| `orderBy` | `string` | Optional | — |
| `view` | `ViewView` | Optional | Optional. Controls whether definitions are populated. |

### Response

`ListViewsResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `views` | `repeated View` | — | — |
| `nextPageToken` | `string` | — | — |

## GetView

Gets a single view.

```protobuf
rpc GetView(GetViewRequest) returns (GetViewResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.ViewService/GetView`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "name": "instances/production/databases/app/schemas/public/views/active_orders"
}
```

### Request

`GetViewRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. The name of the view to retrieve. |
| `view` | `ViewView` | Optional | Optional. Controls whether the definition is populated. |

### Response

`GetViewResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `view` | `View` | — | — |
