---
title: Database service
description: "Service for managing databases within registered Instances."
sidebar:
  label: Database
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

Service for managing databases within registered Instances. This service provides access to database metadata for building visual database browsers and workspace tools.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `DatabaseService`
- **Stability:** `v1alpha1`
- **Source:** [`database.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/database.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.
The JSON examples use protobuf JSON field names and wire encodings. They are request starting points, not production credentials or guaranteed resources.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`ListDatabases`](#listdatabases) | Unary | `ListDatabasesRequest` | `ListDatabasesResponse` |
| [`GetDatabase`](#getdatabase) | Unary | `GetDatabaseRequest` | `GetDatabaseResponse` |
| [`GetDatabaseQueryInsights`](#getdatabasequeryinsights) | Unary | `GetDatabaseQueryInsightsRequest` | `GetDatabaseQueryInsightsResponse` |

## ListDatabases

Lists databases available in a given Instance. Returns databases that the Instance has access to, excluding system databases by default.

```protobuf
rpc ListDatabases(ListDatabasesRequest) returns (ListDatabasesResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.DatabaseService/ListDatabases`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "parent": "instances/production",
  "pageSize": 50
}
```

### Request

`ListDatabasesRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | Required | Required. The parent Instance from which to list databases. Format: instances/\{instance\} |
| `pageSize` | `int32` | Optional | Optional. The maximum number of databases to return. Server may return fewer. If unspecified or 0, server will choose a reasonable default (typically 50). Maximum value is 1000. |
| `pageToken` | `string` | Optional | Optional. A page token received from a previous `ListDatabases` call. Provide this to retrieve the next page of results. |
| `filter` | `string` | Optional | Optional. Filter expression (an AIP-160 subset: `=`, `!=`, the `:` case-insensitive substring operator, combined with `AND`). Values are quoted strings or the bare bools true/false. Filterable fields: - name, owner (string; support the `:` substring operator) - is_system_database (bool) Examples: - is_system_database = false (exclude system databases) - owner = "postgres" - name:"test" - owner = "postgres" AND is_system_database = false |
| `orderBy` | `string` | Optional | Optional. Ordering expression following AIP-132 syntax. Supported fields: name, owner, last_ddl_time, create_time Default: "name asc" Examples: - "name asc" - "last_ddl_time desc, name asc" - "create_time desc" |

### Response

`ListDatabasesResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `databases` | `repeated Database` | — | The list of databases found in the instance. |
| `nextPageToken` | `string` | — | A token to retrieve the next page of results. If this field is empty, there are no subsequent pages. |

## GetDatabase

Gets the details of a specific Database.

```protobuf
rpc GetDatabase(GetDatabaseRequest) returns (GetDatabaseResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.DatabaseService/GetDatabase`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "name": "instances/production/databases/app"
}
```

### Request

`GetDatabaseRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. The name of the database to retrieve. Format: instances/\{instance\}/databases/\{database\} |

### Response

`GetDatabaseResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `database` | `Database` | — | The retrieved database resource. |

## GetDatabaseQueryInsights

Returns live query optimization signals for a Database.

```protobuf
rpc GetDatabaseQueryInsights(GetDatabaseQueryInsightsRequest) returns (GetDatabaseQueryInsightsResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.DatabaseService/GetDatabaseQueryInsights`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "name": "instances/production/databases/app"
}
```

### Request

`GetDatabaseQueryInsightsRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. The name of the database to inspect. Format: instances/\{instance\}/databases/\{database\} |

### Response

`GetDatabaseQueryInsightsResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `queryInsights` | `DatabaseQueryInsights` | — | Live query optimization signals for the database. |
| `partialErrors` | `repeated Status` | — | Errors encountered fetching individual insight categories. The RPC succeeds even when some categories are unavailable. Each Status identifies the failed category via google.rpc.ErrorInfo with metadata key "metric" set to "query_stats" or "table_stats". |
