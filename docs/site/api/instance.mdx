---
title: Instance service
description: "Service for managing connection records to external database instances."
sidebar:
  label: Instance
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

Service for managing connection records to external database instances.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `InstanceService`
- **Stability:** `v1alpha1`
- **Source:** [`instance.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/instance.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`ListInstances`](#listinstances) | Unary | `ListInstancesRequest` | `ListInstancesResponse` |
| [`GetInstance`](#getinstance) | Unary | `GetInstanceRequest` | `GetInstanceResponse` |
| [`CreateInstance`](#createinstance) | Unary | `CreateInstanceRequest` | `CreateInstanceResponse` |
| [`TestInstanceConnection`](#testinstanceconnection) | Unary | `TestInstanceConnectionRequest` | `TestInstanceConnectionResponse` |
| [`UpdateInstance`](#updateinstance) | Unary | `UpdateInstanceRequest` | `UpdateInstanceResponse` |
| [`DeleteInstance`](#deleteinstance) | Unary | `DeleteInstanceRequest` | `DeleteInstanceResponse` |
| [`GetInstanceOverview`](#getinstanceoverview) | Unary | `GetInstanceOverviewRequest` | `GetInstanceOverviewResponse` |
| [`CheckInstanceHealth`](#checkinstancehealth) | Unary | `CheckInstanceHealthRequest` | `CheckInstanceHealthResponse` |
| [`CheckInstanceActivity`](#checkinstanceactivity) | Unary | `CheckInstanceActivityRequest` | `CheckInstanceActivityResponse` |

## ListInstances

Lists registered Instances.

```protobuf
rpc ListInstances(ListInstancesRequest) returns (ListInstancesResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/ListInstances`
- **Mode:** Unary

### Request

`ListInstancesRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `pageSize` | `int32` | Optional | — |
| `pageToken` | `string` | Optional | — |
| `filter` | `string` | Optional | — |
| `orderBy` | `string` | Optional | — |

### Response

`ListInstancesResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `instances` | `repeated Instance` | — | — |
| `nextPageToken` | `string` | — | — |

## GetInstance

Gets the details of a registered Instance.

```protobuf
rpc GetInstance(GetInstanceRequest) returns (GetInstanceResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/GetInstance`
- **Mode:** Unary

### Request

`GetInstanceRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. The name of the instance record to retrieve. Format: instances/\{instance_id\} |

### Response

`GetInstanceResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `instance` | `Instance` | — | — |
| `serverInfo` | `ServerInfo` | — | Live server metadata queried from the PostgreSQL instance. Only populated when the instance connection is active. |
| `partialErrors` | `repeated Status` | — | Error encountered fetching live server metadata. The RPC succeeds (OK) so the instance resource and its latest connection state remain available. ErrorInfo metadata key "metric" is "server_info". |

## CreateInstance

Registers a new external database instance with the platform. The platform will attempt to validate the connection details upon creation.

```protobuf
rpc CreateInstance(CreateInstanceRequest) returns (CreateInstanceResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/CreateInstance`
- **Mode:** Unary

### Request

`CreateInstanceRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `spec` | `CreateInstanceSpec` | Optional | Optional. Compatibility request body for creating an instance. Prefer `instance` for new clients. Exactly one of `spec` or `instance` must be set. |
| `instanceId` | `string` | Optional | Optional. The ID to use for the instance record. |
| `validateOnly` | `bool` | Optional | Optional. If true, validates the connection but does not create the instance. |
| `instance` | `Instance` | Optional | Optional. The instance resource to create. The server ignores `instance.name` and composes the full resource name from `instance_id`. Exactly one of `spec` or `instance` must be set. |

### Response

`CreateInstanceResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `instance` | `Instance` | — | — |

## TestInstanceConnection

Tests PostgreSQL connection details without creating or updating an Instance.

```protobuf
rpc TestInstanceConnection(TestInstanceConnectionRequest) returns (TestInstanceConnectionResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/TestInstanceConnection`
- **Mode:** Unary

### Request

`TestInstanceConnectionRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `config` | `PostgresConfig` | Required | Required. PostgreSQL connection configuration to test. |

### Response

`TestInstanceConnectionResponse` has no fields.

## UpdateInstance

Updates the connection details for a registered Instance.

```protobuf
rpc UpdateInstance(UpdateInstanceRequest) returns (UpdateInstanceResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/UpdateInstance`
- **Mode:** Unary

### Request

`UpdateInstanceRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `instance` | `Instance` | Required | Required. The instance record to update. Only the fields specified in update_mask are modified; unset fields are ignored. |
| `updateMask` | `FieldMask` | Required | Required. The list of fields to be updated. |

### Response

`UpdateInstanceResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `instance` | `Instance` | — | — |

## DeleteInstance

De-registers an Instance, removing its connection record from the platform. This does not affect the external database itself.

```protobuf
rpc DeleteInstance(DeleteInstanceRequest) returns (DeleteInstanceResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/DeleteInstance`
- **Mode:** Unary

### Request

`DeleteInstanceRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. The name of the instance record to delete. Format: instances/\{instance_id\} |

### Response

`DeleteInstanceResponse` has no fields.

## GetInstanceOverview

Returns a live health overview for a PostgreSQL instance. Metrics are queried directly from the instance and cached briefly.

```protobuf
rpc GetInstanceOverview(GetInstanceOverviewRequest) returns (GetInstanceOverviewResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/GetInstanceOverview`
- **Mode:** Unary

### Request

`GetInstanceOverviewRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. Resource name of the instance. Format: instances/\{instance_id\} |

### Response

`GetInstanceOverviewResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `instanceOverview` | `InstanceOverview` | — | — |
| `partialErrors` | `repeated Status` | — | Errors encountered fetching individual metric categories. The RPC succeeds (OK) even when some metrics are unavailable. Each Status identifies the failed category via google.rpc.ErrorInfo in its details, with metadata key "metric" set to the category name (one of: "connections", "storage", "cache", "io"). |

## CheckInstanceHealth

Checks live PostgreSQL health signals that are inexpensive and directly observable from ordinary database views/settings.

```protobuf
rpc CheckInstanceHealth(CheckInstanceHealthRequest) returns (CheckInstanceHealthResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/CheckInstanceHealth`
- **Mode:** Unary

### Request

`CheckInstanceHealthRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. Resource name of the instance. Format: instances/\{instance_id\} |

### Response

`CheckInstanceHealthResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `health` | `InstanceHealth` | — | — |
| `partialErrors` | `repeated Status` | — | Errors encountered fetching individual health categories. The RPC succeeds (OK) even when some categories are unavailable. Each Status identifies the failed category via google.rpc.ErrorInfo in its details, with metadata key "check" set to the category name (one of: "connection_activity", "replication", "stats_access", "pg_stat_statements", "autovacuum"). |

## CheckInstanceActivity

Checks only live connection activity for high-frequency session polling.

```protobuf
rpc CheckInstanceActivity(CheckInstanceActivityRequest) returns (CheckInstanceActivityResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.InstanceService/CheckInstanceActivity`
- **Mode:** Unary

### Request

`CheckInstanceActivityRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | Required | Required. Resource name of the instance. Format: instances/\{instance_id\} |

### Response

`CheckInstanceActivityResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `activity` | `ConnectionActivityHealth` | — | — |
| `partialErrors` | `repeated Status` | — | Errors encountered fetching connection activity. The RPC succeeds (OK) when activity is unavailable so the page can show the category reason. ErrorInfo metadata key "check" is "connection_activity". |
