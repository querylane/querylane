---
title: Console service
description: "ConsoleService provides methods to retrieve metadata about the Querylane Console application itself."
sidebar:
  label: Console
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

ConsoleService provides methods to retrieve metadata about the Querylane Console application itself.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `ConsoleService`
- **Stability:** `v1alpha1`
- **Source:** [`console.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/console.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.
The JSON examples use protobuf JSON field names and wire encodings. They are request starting points, not production credentials or guaranteed resources.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`GetConsoleConfig`](#getconsoleconfig) | Unary | `GetConsoleConfigRequest` | `GetConsoleConfigResponse` |

## GetConsoleConfig

Retrieves static, server-provided configuration and metadata about the console instance, such as build information and capabilities.

```protobuf
rpc GetConsoleConfig(GetConsoleConfigRequest) returns (GetConsoleConfigResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.ConsoleService/GetConsoleConfig`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{}
```

### Request

`GetConsoleConfigRequest` has no fields.

### Response

`GetConsoleConfigResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `buildInfo` | `BuildInfo` | Output only | Version and build information for the application. |
| `databaseStatus` | `AppDatabaseStatus` | Output only | Current state of the application's metadata database. |
| `instanceManagementMode` | `InstanceManagementMode` | Output only | How managed PostgreSQL instances are configured (API or config file). |
| `configFilePath` | `string` | Output only | Path to the server configuration file when one is active. |
