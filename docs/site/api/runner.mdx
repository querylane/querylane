---
title: Runner service
description: "RunnerService exposes the scheduling state of querylane's background jobs: the sampling probes and maintenance jobs that run against registered instances."
sidebar:
  label: Runner
---

{/* Generated by `bun run docs:api:generate`. Edit the protobuf source, not this page. */}

RunnerService exposes the scheduling state of querylane's background jobs: the sampling probes and maintenance jobs that run against registered instances. It answers "is sampling healthy?" — which probes ran, when they last succeeded per target, and what the last failure was.

- **Package:** `querylane.console.v1alpha1`
- **Service:** `RunnerService`
- **Stability:** `v1alpha1`
- **Source:** [`runner.proto`](https://github.com/querylane/querylane/blob/main/proto/querylane/console/v1alpha1/runner.proto)

The field tables show the top-level wire shape. Follow the source link for nested messages, enums, validation constraints, and resource annotations.
The JSON examples use protobuf JSON field names and wire encodings. They are request starting points, not production credentials or guaranteed resources.

## Endpoints

| RPC | Mode | Request | Response |
| --- | --- | --- | --- |
| [`ListRunnerExecutions`](#listrunnerexecutions) | Unary | `ListRunnerExecutionsRequest` | `ListRunnerExecutionsResponse` |

## ListRunnerExecutions

Lists background runner executions: one entry per (runner, target) pair.

```protobuf
rpc ListRunnerExecutions(ListRunnerExecutionsRequest) returns (ListRunnerExecutionsResponse)
```

- **Procedure:** `POST /querylane.console.v1alpha1.RunnerService/ListRunnerExecutions`
- **Mode:** Unary

### JSON request example

The values are illustrative. Replace resource names, identifiers, and credentials for your deployment.

```json
{
  "pageSize": 50
}
```

### Request

`ListRunnerExecutionsRequest`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `pageSize` | `int32` | Optional | — |
| `pageToken` | `string` | Optional | — |
| `filter` | `string` | Optional | Optional. AIP-160 filter. Filterable fields: runner_name, target. Example: `target = "instances/prod"` or `runner_name = "probe_cache"`. |
| `orderBy` | `string` | Optional | Optional. AIP-132 order_by. Defaults to `runner_name, target`. |

### Response

`ListRunnerExecutionsResponse`

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `runnerExecutions` | `repeated RunnerExecution` | — | — |
| `nextPageToken` | `string` | — | — |
