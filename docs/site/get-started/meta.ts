import { defineMeta } from "blume";

export default defineMeta({
	title: "Get started",
	icon: "rocket",
	order: 1,
	pages: [
		"index",
		"local-preview",
		"embedded-postgresql",
		"external-postgresql",
		"manual-yaml",
		"register-instance",
	],
});
