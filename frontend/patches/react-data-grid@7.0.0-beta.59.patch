diff --git a/lib/index.js b/lib/index.js
index 67b9df7..e661ffc 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -785,0 +786,2 @@ function useGridDimensions() {
+		let resizeTimeoutId;
+		let latestSize;
@@ -787,5 +789,11 @@ function useGridDimensions() {
-			const size = entries[0].contentBoxSize[0];
-			const { clientHeight: clientHeight$1, offsetHeight: offsetHeight$1 } = gridRef.current;
-			flushSync(() => {
-				setInlineSize(size.inlineSize);
-				setBlockSize(size.blockSize);
+			latestSize = entries[0].contentBoxSize[0];
+			if (resizeTimeoutId !== void 0) return;
+
+			// Coalesce continuous container resizes instead of forcing a React commit
+			// for every ResizeObserver delivery. The latest dimensions are always
+			// applied, while keeping column virtualization responsive during a drag.
+			resizeTimeoutId = window.setTimeout(() => {
+				resizeTimeoutId = void 0;
+				const { clientHeight: clientHeight$1, offsetHeight: offsetHeight$1 } = gridRef.current;
+				setInlineSize(latestSize.inlineSize);
+				setBlockSize(latestSize.blockSize);
@@ -793 +801 @@ function useGridDimensions() {
-			});
+			}, 50);
@@ -797,0 +806 @@ function useGridDimensions() {
+			window.clearTimeout(resizeTimeoutId);
