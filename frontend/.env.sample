# Only needed for local development (frontend dev server → backend on port 8080).
# In production, the frontend is embedded in the backend and uses same-origin requests.
PUBLIC_API_BASE_URL=http://localhost:8080
